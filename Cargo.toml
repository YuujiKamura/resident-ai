[package]
name = "resident-ai"
version = "0.2.0"
edition = "2021"
description = "Persistent AI CLI session via ACP (JSON-RPC 2.0 over stdio)"

[dependencies]
base64 = "0.22"
serde_json = "1"

[[bin]]
name = "cli-compat"
path = "src/bin/cli-compat.rs"
